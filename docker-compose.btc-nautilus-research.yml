# -----------------------------------------------------------------------------
# DEPRECATED — **do not merge** this file. Services live in ``docker-compose.yml``
# under profile **``btc-nautilus``** (``freqtrade-btc-spot`` + ``nautilus-research``).
#
#   docker compose --profile btc-nautilus up -d --build
#
# Typical full node:
#   docker compose --profile main-traders --profile btc-0-1 --profile btc-nautilus up -d --build
# -----------------------------------------------------------------------------
services: {}
