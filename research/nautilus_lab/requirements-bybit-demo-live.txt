# Nautilus live Bybit **demo** adapter (host venv; no Nautilus image in default compose).
#   cd research/nautilus_lab && python3 -m venv .venv && . .venv/bin/activate
#   pip install -r requirements-bybit-demo-live.txt
#
nautilus_trader>=1.211.0,<2
pandas>=2.0
numpy>=1.26
requests>=2.28
# Live bar-node ML (btc_predict_live / btc_predict_runner)
scikit-learn>=1.3.0
xgboost>=2.0.0
