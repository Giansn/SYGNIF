# Local dev + CI (not baked into Freqtrade Docker image)
# Install: pip install -r requirements.txt -r requirements-dev.txt
# Or: uv pip install -r requirements.txt -r requirements-dev.txt

ruff>=0.9.0
pytest>=8.3.0
pytest-cov>=6.0.0
pytest-xdist>=3.6.0

# REPL / ergonomics
ipython>=8.30.0
rich>=13.9.0

# Modern HTTP (scripts, small tools; tests may use requests via deps)
httpx>=0.28.0

# Optional: faster resolver / venv (CI still uses pip)
uv>=0.6.0

# system_snapshot_shot.py — also run: playwright install chromium && playwright install-deps chromium
playwright>=1.49.0

# scripts/predict_protocol_gate_optimizer.py — Bayesian-style gate search (optional)
optuna>=4.0.0
# pyswarms>=2.7.0  # optional: --engine pyswarms
